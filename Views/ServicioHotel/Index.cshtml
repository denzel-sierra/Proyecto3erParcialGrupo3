@model IEnumerable<ServicioHotel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<hr class="mt-5" />
<hr class="mt-5" />
<h3 class="text-center">Historial de Servicio Hotel</h3>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreServicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Duracion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tarifa)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreServicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duracion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tarifa)
            </td>
            
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDServicio">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDServicio">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDServicio">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
